[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "ls -la dist/ || echo 'dist/ is empty or missing'"]

[start]
cmd = "node dist/main"
